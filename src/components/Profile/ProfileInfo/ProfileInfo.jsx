import s from "./ProfileInfo.module.css"


const ProfileInfo = () => {
    return (
        <div className={s.profileInfo}>
            avatar + description
        </div>
    );
}

export default ProfileInfo;